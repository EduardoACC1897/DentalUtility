using UnityEngine;

public class CardTooth : MonoBehaviour
{
    public bool grindAction = true;  // Moler
    public bool cutAction = false;  // Cortar
    public bool tearAction = false;  // Desgarrar
    private int dirty = 0;  // Cantidad de suciedad
    private int caries = 0;  // Nivel de caries

    // Update is called once per frame
    void Update()
    {

    }
}
